export class Cat {
    url: string;
    id: string;
    numVotes: number;

        constructor(url: string, id: string, numVotes: number) {
        this.url = url;
        this.id = id;
        this.numVotes = numVotes;
    }
}
